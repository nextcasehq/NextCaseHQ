# Pending Integration Register

This register tracks deferred integration, testing, and optimization work for NextCaseHQ Modules 13–20.

## Resolved: Former Critical Production Blocker (Admin Panel Authentication)

**Resolved by PR #65 (commit `4b41b98`).** The admin console's authentication used to be entirely client-side (`apps/web/src/app/admin/page.tsx` checked a hardcoded password — one valid value was literally the word `admin` — and wrote a hardcoded `NEXTCASE_ADMIN_TOKEN` cookie value itself). It is now server-verified: `POST /api/admin/session` checks the submitted key against `ADMIN_ACCESS_TOKEN` (constant-time, rate-limited, origin-checked) and mints a signed, httpOnly, Secure, SameSite=Strict session cookie; `middleware.ts`'s `/api/admin/*` gate verifies that signed token. The admin UI's markup/labels/tabs are unchanged — only the login/logout/session-check wiring changed. See `known_limitations.admin_auth_shared_secret_no_individual_accountability` in `docs/BUILD_LEDGER.json` for the one tradeoff this milestone deliberately left open (a single shared operator secret, not per-admin accounts).

| Module | Task | Integration Status | Reason for Deferral | Priority |
|---|---|---|---|---|
| 13 | Edge Webhook Byte Handling | COMPLETED | Type reconciliation complete | Medium |
| 14 | Design Token Mapping | COMPLETED | Cross-package linking complete | Medium |
| 15 | Observability/Audit Logic | COMPLETED | Cross-package linking complete | High |
| 16 | Messaging Delivery Fallback | COMPLETED | Cross-package linking complete | Medium |
| 17 | Advanced File Ingestion | COMPLETED | Type reconciliation complete | High |
| 18 | US FRCP Country Pack | COMPLETED | Cross-package linking complete | Medium |
| 19 | UK CPR Country Pack | COMPLETED | Cross-package linking complete | Medium |
| 20 | Deployment Topology | COMPLETED | Scaffolding complete | Medium |
| ALL | Security Audit Validation | PENDING | Deferred to Sprint B | High |
| ALL | Performance Validation | PENDING | Deferred to Sprint B | High |
| ALL | UI Polishing | PENDING | Deferred to Sprint B | Low |
| DB-1 | Real PostgreSQL Connection (Foundation Milestone) | COMPLETED | In-memory `DatabaseClient` simulation replaced with a real `pg`-backed client; `db/schema.sql` is now applied via `scripts/db/migrate.js` and enforces tenant RLS with `FORCE ROW LEVEL SECURITY`. Verified against a real PostgreSQL instance, not simulated. Supabase project provisioning still required for staging/production (see PR description). | High |
| DB-2 | Real Authentication + Persistent User Storage | COMPLETED | `POST /api/auth/session` now verifies bcrypt-hashed credentials against a real `User` row and mints a signed, httpOnly JWT session cookie; `/dashboard/*` is now actually gated by middleware on that cookie (previously not enforced at all despite appearing to be — see PR description); logout invalidates the session. Verified against real PostgreSQL, bcryptjs, and a running server, not simulated. Supabase project provisioning still required for staging/production. | High |
| DB-3 | Server-Enforced Tenant Authorization (Upload Route) | COMPLETED | `POST /api/documents/upload` now derives tenant identity exclusively from the verified session cookie; any client-supplied `x-nextcase-tenant-id`/`x-tenant-id` header is ignored entirely. Verified end-to-end against a real running server: an authenticated Tenant A request sending forged headers claiming Tenant B still resolves to Tenant A. Middleware's disconnected Bearer-token gate is bypassed specifically for this route since it now authorizes itself; no other route's authorization behavior changed. Scope was intentionally limited to this one existing route — `/api/webhooks` and all general domain CRUD were explicitly out of scope. | High |
| DB-4 | Secure Webhook Verification | COMPLETED | `POST /api/webhooks` now requires an HMAC-SHA256 signature (`x-nextcase-signature`) over `${timestamp}.${rawBody}` plus a fresh `x-nextcase-timestamp` (5-minute tolerance); unsigned, invalid, tampered, expired, and replayed requests are all rejected before the body is parsed as JSON. Replay protection is an in-memory guard scoped to the tolerance window — sufficient for a single process, but a shared store (Redis/DB) is needed for correct protection across multiple horizontally-scaled Edge instances (flagged, not silently overstated). Middleware's disconnected Bearer-token gate is bypassed for this route too, same pattern as DB-3. Verified end-to-end against a real running server with real HMAC signatures. | High |
| DB-5 | Production Security Hardening (scoped) | COMPLETED — scoped work only, production security is NOT fully complete | Security headers (CSP, HSTS, X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy) applied globally via `next.config.ts`, using a static (non-nonce) CSP after real browser testing showed nonce-based CSP breaks this app's statically-prerendered pages — blocks external script loading but not inline-script injection (documented limitation, not silently overstated). In-memory, per-process rate limiting on login and webhook routes (needs a shared store for a multi-instance deployment — same limitation class as the PR #62 replay guard). Origin/Referer-based CSRF checks on login/logout/upload. Tenant-context cookie now sets `Secure` over HTTPS. Startup env/secret validation (`instrumentation.ts`) refuses to boot in production on a missing or known-insecure `JWT_SECRET`/`WEBHOOK_SIGNING_SECRET`/`ADMIN_ACCESS_TOKEN`. The previously fully-hardcoded, git-visible admin token is now env-configurable and constant-time compared. **Does not fix the admin panel's client-side login itself — see Critical Production Blocker above.** Merged: commit `05b4588`. | High |
| DB-6 | Server-Verified Admin Authentication | COMPLETED | Replaced the client-shipped password/token gate with a real server-verified admin login (`POST/GET /api/admin/session`, `POST /api/admin/logout`), a signed httpOnly/Secure/SameSite=Strict session cookie, and a middleware gate that verifies that signed token instead of a raw shared secret. Admin UI markup/labels/tabs unchanged; regular user auth (`lib/auth/session.ts`, `jwt.ts`, `User` table) untouched. Merged: commit `4b41b98`. Remaining tradeoff (single shared operator secret, no per-admin accountability) recorded as a known limitation, not silently closed. | High |
| DB-7 | Distributed Rate Limiting (Redis) — login & admin-login | COMPLETED — Node.js-runtime routes only | `POST /api/auth/session` and `POST /api/admin/session` now share rate-limit state via Redis (`lib/security/redis-rate-limit.ts`, `INCR`+`PEXPIRE` fixed-window counter identical in semantics to the prior in-memory version) when `REDIS_URL` is set, falling back to the in-memory limiter otherwise; required in production by `env-validation.ts`. Verified against a real Redis instance: two independent `next start` processes on different ports were shown to share the same exhausted rate-limit counter for one client IP. **Does not cover `POST /api/webhooks`** — see "Decision Needed" below. | High |
| DB-8 | Real Case Management API (LegalCase) | COMPLETED — backend only | `POST/GET /api/cases` and `GET/PATCH/DELETE /api/cases/[id]` now persist real rows in the existing `LegalCase` table (schema/RLS already present). Tenant identity comes exclusively from the verified session cookie; a real case ID belonging to another tenant returns 404 on GET/PATCH/DELETE alike (RLS-backed, not app-level filtering). Verified end-to-end against a real running server for two separate tenants. **The `/cases`, `/cases/[id]`, `/cases/new`, `/matters` pages still render mock data** — wiring them to this real API is a separate follow-up milestone, deliberately not bundled here. | High |
| DB-9 | Case Management API Hardening | COMPLETED | Closed the gap between DB-8's initial CRUD and production readiness, per explicit Product Owner priority to finish the backend before starting UI work: consistent try/catch error handling on all `/api/cases`/`/api/cases/[id]` handlers (matching `/api/webhooks`/`/api/documents/upload` convention), and real pagination on `GET /api/cases` (`?limit=`/`?offset=`, validated, with a true tenant-scoped `total` count) replacing the prior hardcoded `LIMIT 100` with no way to see more. Verified against a real running server and real Postgres. | High |
| DB-10 | LegalCase Schema Alignment (status/court/judge/stage/hearing_date/notes) | COMPLETED | Product Owner-approved architectural decision: added real, additive columns to `LegalCase` (status CHECK-constrained to PENDING/HEARING/DISPOSED/APPEAL, court, judge, stage, hearing_date as TEXT, notes) rather than folding them into `state_metadata` JSON or building a full Matter table. Migration verified idempotent (re-ran twice, zero errors) and preserves existing rows. RLS untouched, still covers the new columns. **Matter is explicitly out of scope** — no Matter table, no matter_id column. | High |
| DB-11 | Cases UI Wired to Real API | COMPLETED | `apps/web/src/app/cases/page.tsx` and `apps/web/src/app/cases/[id]/page.tsx` now call the real `/api/cases`/`/api/cases/[id]` endpoints instead of `lib/db/litigation-db.ts`'s mock store. Real loading + 401-unauthenticated states added. The "Linked/Bound Parent Matter" UI was removed (not faked) since no Matter table exists. Verified with a real running server and a real headless-browser session: create → list → status-filter → detail → notes-save → reload-persists, zero console errors. `lib/db/litigation-db.ts`, `/cases/new`, and all `/matters` pages are untouched — only the two pages named for this milestone were modified. | High |
| DB-12 | Document Envelope Persistence (metadata) | COMPLETED — metadata only, object storage still pending | `POST /api/documents/upload` now persists a real `DocumentEnvelope` row instead of a fake UUID. Added `GET /api/documents` (paginated, `?case_id=` filter) and `GET/DELETE /api/documents/[id]`, tenant-scoped the same way as `/api/cases`. **Real bug found and fixed via live testing**: Postgres FK constraint checks always bypass RLS by design, so the `case_id` FK alone let a cross-tenant `case_id` through — fixed with an explicit RLS-scoped ownership check before insert, verified both in tests and against a real running server. `storage_structure` honestly records only byte count and a "pending" storage marker — no file bytes are durably stored yet. | High |
| DB-13 | Object Storage Integration | COMPLETED | `POST /api/documents/upload` now durably stores real file bytes via a provider-agnostic `@aws-sdk/client-s3` adapter (`lib/storage/object-storage.ts`) — works against any S3-compatible endpoint (AWS S3, Supabase Storage, MinIO, R2, etc.) via env vars only, no vendor-specific code. Added `GET /api/documents/[id]/download` (tenant-scoped) and extended `DELETE` to remove the real object too. File-type allowlist + path-traversal-safe, tenant-namespaced object keys. Object storage is a hard requirement once configured (503 if unset, required in production). **MinIO could not run in this environment** (Docker Hub pull failed; `dl.min.io` binary download blocked by egress policy) — investigated and used `s3rver` instead, a real (not mocked) S3-API-compatible local server, verified via a working AWS-SDK smoke test before adopting it. `scripts/test/start-s3rver.js` runs it for local dev/CI. Verified end-to-end against a real running server + real Postgres + real s3rver: upload → byte-for-byte download → cross-tenant 404 (storage never touched) → disallowed file type rejected → delete removes both the DB row and the real object. | High |
| DB-14 | Outbound Messaging (email/SMS) | PENDING | Product Owner has selected the vendors (Resend for email, Twilio for SMS) and directed building provider abstractions up to the real-credentials boundary — see DB-20 for the completed Payments counterpart. Matter table + matter_id linkage (DB-10's deferred scope) also remains pending. | High |
| DB-19 | UI/UX Platform Refinement Directive (first incremental series) | COMPLETED | Product Owner-issued platform-wide visual/usability directive (explicitly not a feature/workflow/architecture change): above-the-fold landing content, unified gold/maroon/brown/black/cream design language, no visually empty pages, subtle premium backgrounds, avoidance of gaudy effects, tasteful empty states — delivered as 4 incremental PRs, each independently verified (full test suite + typecheck + build + real production-server screenshots at multiple viewport sizes) before merging, per the Product Owner's explicit instruction. (1) PR #77: hero above-the-fold spacing fix (pre-dates the directive, re-confirmed compliant). (2) PR #79: shared `BrandBackground` component (soft gold wash + scales-of-justice watermark) on login/dashboard/cases/matters — found and fixed a real CSS stacking-context bug (`isolate` needed; a bare negative z-index escapes past a `position: relative` ancestor that doesn't itself establish a stacking context, rendering invisibly behind the document root). (3) PR #80: toned down 3 overly heavy hero shadow values per the directive's "avoid gaudy/heavy shadows" guidance; left modal/drawer `shadow-2xl` alone as a legitimate, non-gaudy convention for true overlays. (4) PR #81: shared `EmptyState` component replacing bare OS emoji (incl. an off-brand blue-rendering scales-of-justice glyph) with a consistent gold-tinted icon badge across cases/matters/notifications, each with a wired create-action. **Standing directive**: this is an ongoing constraint on all future UI work, applied incrementally as pages are touched, not a one-time pass — admin console and public marketing subpages beyond the hero were not touched in this round and remain candidates for a future pass. | High |
| DB-20 | Payments Provider Abstraction (Stripe first) | COMPLETED — up to the real-credentials boundary | Product Owner-selected architecture: provider-agnostic `PaymentProvider` interface (`lib/billing/types.ts`), Stripe as the first (and, today, only) implementation (`lib/billing/providers/stripe-provider.ts`, official `stripe` package) — no vendor SDK imported outside that file. Selected via `PAYMENT_PROVIDER` + `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` (`lib/billing/payment-provider.ts`); retry logic is Stripe's own SDK built-in exponential backoff (`maxNetworkRetries`) rather than a redundant second layer. `POST /api/billing/checkout` creates a real Stripe Checkout Session for a tenant wallet top-up, carrying the tenant id through as session metadata. `POST /api/billing/webhook` verifies Stripe's real webhook signature scheme and, on `checkout.session.completed`, credits `TenantWallet.balance` + records a `WalletTransactionRecord` — idempotently, keyed on the Stripe checkout session id. Verified: 24 new tests (Stripe SDK mocked at the module level for the provider; the webhook route's DB-crediting logic tested against a real Postgres instance) plus the full 266-test suite, zero regressions. Real production-server verification of the actual Stripe SDK (not mocked): checkout correctly surfaces a real Stripe API failure as a clean 502 (the real-credentials boundary); webhook signature verification is pure local HMAC, so it was fully verified for real — a hand-constructed genuinely valid Stripe-style signature correctly credits the wallet (123400 paise -> a real Postgres balance of 1234.00) and replaying the same event is idempotent. No API keys or account identifiers hardcoded anywhere. Excludes subscription/recurring billing, refunds, and any dashboard UI entry point for triggering a checkout — backend abstraction only. | High |
| DB-18 | AI Provider Abstraction + RAG (OpenAI first, Anthropic second) | COMPLETED — up to the real-credentials boundary | Product Owner-selected architecture: provider-agnostic `LLMProvider` interface (`lib/ai/types.ts`), OpenAI as the first implementation and Anthropic as the second (`lib/ai/providers/*`, official `openai`/`@anthropic-ai/sdk` packages), selected via `AI_PROVIDER` + per-vendor API key env vars (`lib/ai/llm-provider.ts`) — no vendor SDK imported anywhere outside `lib/ai/providers/`. Exponential-backoff retry (`lib/ai/retry.ts`) on 429/5xx only, never on a 4xx that would fail identically every retry. RAG answer generation (`lib/ai/rag.ts`) reuses the existing pgvector/full-text hybrid search (extracted into `lib/search/hybrid-search.ts`), returns a defined `NO_CONTEXT_FOUND` result without calling the LLM at all when nothing relevant is indexed, and otherwise grounds the answer in numbered, cited source excerpts. `POST /api/ai/ask` exposes this; the dashboard's AI Dialogue Stream (previously dead code — its send handler wasn't even wired to its form submit) now calls it for real. Verified: 33 new tests including OpenAI/Anthropic provider behavior with the vendor SDKs mocked at the module level, full 242-test suite passing, and real production-server verification confirming the exact boundary the Product Owner specified — 422 with no indexed context, 503 once indexed context exists but no vendor key is set, correct 401/400 for auth/validation. No API keys or account identifiers hardcoded anywhere. | High |
| DB-17 | Billing & Messaging Foundations | COMPLETED — foundations only | Fixed a real security gap: `WalletTransactionRecord` had no `tenant_id`/RLS at all, relying solely on the `wallet_id` FK, which (like every FK) bypasses RLS for its own referential-integrity check. Added `tenant_id` + `ENABLE`/`FORCE ROW LEVEL SECURITY` + `tenant_isolation_policy`, additive and idempotent. Added `GET /api/wallet` (lazily creates a zero-balance wallet on first read) and `GET /api/wallet/transactions` (paginated, tenant-scoped ledger). Added a new `Notification` table (tenant_id + RLS, nullable user_id for tenant-wide vs. per-user notifications) with `GET /api/notifications` and `PATCH /api/notifications/[id]`, and rewired the dashboard's notification bell from hardcoded mock data to this real API. Verified: 18 new tests + full 191-test pre-existing suite (209 total) passing; real production-server verification including a real headless-browser screenshot of the bell rendering live data. **Explicitly excludes** real payment collection and real outbound email/SMS delivery — both need a Product Owner vendor decision + credentials (see DB-14) — and nothing yet produces real `Notification` rows from application events, since there's no real billing/hearing-reminder event to notify about yet. | High |
| DB-15 | Document Search & Indexing (pgvector hybrid search) | COMPLETED | Product Owner-approved architecture: PostgreSQL + pgvector only (no Elasticsearch/OpenSearch/Meilisearch/Typesense), reusing `DocumentChunkVector`. That table previously had no `tenant_id`/RLS at all and no code ever wrote to it — extended additively with `tenant_id` (+ its own `tenant_isolation_policy`, `ENABLE`+`FORCE ROW LEVEL SECURITY`), `content`, a generated+GIN-indexed `content_tsv` (full-text half), and a real `vector(1536)` `embedding` column with an HNSW index (`vector_cosine_ops`, similarity half). Indexing pipeline (`lib/search/indexing.ts`, exposed as `POST /api/documents/[id]/index`): RLS-scoped envelope fetch → real stored object fetched via the existing S3 adapter → pluggable plain-text extraction (`lib/search/text-extraction.ts` — only `text/plain` today; PDF/DOC/image return a defined `UNSUPPORTED_CONTENT_TYPE` skip, not a fake success, so OCR/parsers can plug in later without redesign) → fixed-size overlapping chunking → one embedding per chunk via a provider-agnostic abstraction. Embedding provider (`lib/search/embedding-provider.ts`): no AI vendor hardcoded — a generic HTTP client speaks the OpenAI embeddings API shape when `EMBEDDING_API_BASE_URL`/`EMBEDDING_API_KEY` are set (works against OpenAI or any compatible self-hosted/gateway server); otherwise a deterministic local "feature hashing" provider keeps the full pipeline functional in dev/CI without a live vendor key (soft, non-blocking limitation — real semantic quality needs those vars set in production). Hybrid search (`GET /api/search?q=&case_id=&limit=&offset=`) fuses vector-similarity and full-text rankings via Reciprocal Rank Fusion (k=60), avoiding cross-scale score normalization. Tenant isolation enforced throughout via `DocumentChunkVector`'s own RLS policy — the `envelope_id` FK to `DocumentEnvelope` is never relied on alone, per the FK-bypasses-RLS lesson from DB-12. Verified: pgvector 0.6.0 installed and `CREATE EXTENSION`'d in a real local Postgres (not assumed); schema migration re-run twice confirming idempotency; 26 new automated tests plus the full pre-existing 191-test suite passing with zero regressions; full production-mode runtime verification against a real `next start` server with real session JWTs — upload → index → search-finds-the-right-chunk-with-a-higher-fused-score, cross-tenant index-attempt 404s, cross-tenant search never returns the other tenant's chunks even via vector-only fallback ranking, unauthenticated index-attempt 401s. Explicitly excluded per Product Owner instruction: LLM chat, RAG answer generation, OCR, AI summarization, UI redesign, external search engines. No changes to the pre-existing `/search`/`/dashboard/search` stub pages or the unused `packages/search-engine` scaffold. | High |
| DB-16 | Brand Color Uniformity + Landing Page Visual Fixes | COMPLETED | Product Owner-reported bugs fixed: a blank "white patch" on the landing page (redundant top padding on `<main>` stacked on top of the `sticky` navbar's own height), a hero background image nearly invisible under an 85-95%-opaque overlay (reduced to 25-80%), and a hero "product preview" panel that was a static AI screenshot with a blank canvas, a stray monitor-stand graphic, and off-brand blue/indigo "LexCase AI" branding (replaced with a real JSX mockup in the site's own gold/brown/cream palette). Every internal page (dashboard shell, admin console + components, cases, matters, login, the TriPaneChamber AI workspace) had its indigo/blue/gray/slate/neutral Tailwind tokens remapped to the gold/maroon/brown/cream/black brand palette already used on the landing page. Also fixed a latent bug where `tailwind.config.ts`'s `bg-bg-base`/`text-brand` utilities referenced CSS custom properties that were never actually declared, silently resolving to no color. Verified: full 191-test suite, typecheck, and production build all pass (styling-only, zero behavioral changes); real production server + headless-browser before/after screenshots across landing, login, cases, matters, dashboard, and admin. **Standing directive**: the Product Owner has established gold/maroon/brown/black/light-cream as the platform's single design language going forward, applied incrementally as pages are touched during implementation rather than as a dedicated redesign — this is not a one-time fix but an ongoing constraint on all future UI work. | High |
| DB-22 | UI/UX Directive Validation Pass (sign-off audit) | COMPLETED — 2 bugs fixed; 2 larger pre-existing gaps flagged, see Decision Needed below | Product Owner requested a full validation-and-sign-off pass against the running app for the earlier UI/UX refinement directive (DB-19), rather than assuming the prior PRs fully satisfied it. Real headless-Chromium screenshots captured for every public/auth page at 4 breakpoints (1920x1080 desktop, 1366x768 13in laptop, 768x1024 tablet, 390x844 mobile) plus full-page captures. Found and fixed 2 confirmed bugs: (1) `/docs`, `/privacy`, `/resources`, `/terms` each rendered `<Navbar />` twice — once from the global `NavbarWrapper` in root layout, once redundantly from the page itself — fixed by removing the redundant local render on all four; (2) `/login`'s "HQ" wordmark rendered in grey (`#111111]/60`) instead of the brand gold used identically on every other page's Navbar (`#E4C77E`) — recolored to `#8A6D2F` (a darker gold already used elsewhere against light backgrounds, for contrast). Verified: full 266-test suite passing (zero regressions, pure JSX/class-name diffs), `pnpm typecheck` and `pnpm build` both pass, real before/after screenshots confirm both fixes. | High |

## ⚠ Decision Needed: Webhook Rate Limit / Replay Protection on Edge Runtime

`POST /api/webhooks` runs on the Edge runtime (`export const runtime = 'edge'`), where `ioredis` (a raw-TCP client) cannot run — the Edge runtime has no `net`/`tls` modules. DB-7 above closed the shared-state gap for the two Node.js-runtime routes only; the webhook route's rate limit and replay guard (`lib/security/rate-limit.ts`, `lib/security/replay-guard.ts`) are still in-memory/per-instance. Two real options, neither chosen unilaterally:
- **(A)** Adopt an Edge-compatible REST-based Redis client (e.g. Upstash Redis or Vercel KV) for this route specifically — a concrete vendor/account choice.
- **(B)** Move `POST /api/webhooks` off the Edge runtime to Node.js runtime so the same `ioredis`-backed limiter can cover it too — an architecture change to a route explicitly built as Module 13 ("Edge Webhook Byte Handling").

Awaiting Product Owner decision before implementing either.

## ✅ Resolved: Two Findings from the UI/UX Directive Validation Pass (DB-22)

Both items flagged by DB-22 were resolved by direct Product Owner decision:

1. **Marketing subpages brand treatment — APPROVED and completed.** See DB-23 below.
2. **Primary dark brand color (`#0E241B`) — RETAINED as an approved palette color.** The Product Owner confirmed it reads as a premium near-black and contributes positively to the platform's identity; it is now a permanent, named part of the palette ("Deep Forest Green"), not a deviation. The refined approved palette is: Gold, Deep Forest Green (`#0E241B`), Maroon (used selectively where it improves the design, not forced for compliance), Brown, Light Cream, and White for content surfaces. No code change was made for this item.

| DB-23 | Marketing Pages Brand Refinement | COMPLETED | Product Owner-approved follow-up to DB-22's flagged gap: full visual refinement of `/about`, `/features`, `/pricing`, `/solutions`, `/contact` to match the design system already established on the landing/auth pages — IA, copy, navigation, and functionality all preserved, no redesign. Replaced every indigo/neutral Tailwind class with the approved palette; replaced all raw OS emoji with minimal line-art SVG icons matching the existing `EmptyState` icon-badge visual language; removed the `flex justify-center` + redundant `pt-16` layout pattern that was the actual cause of the large blank areas (root layout.tsx already carries a code comment warning against exactly this anti-pattern); switched every page to the shared `Footer`/`Badge`/`Card` components (also fixing 4 broken footer links that pointed at `/login` instead of the real `/privacy`/`/terms` pages). Verified: full 266-test suite passing, zero regressions; `pnpm typecheck` and `pnpm build` both pass; grep-verified zero remaining indigo/neutral/blue/slate/gray classes and zero raw emoji across all public/auth pages; real production-server screenshots across all 4 breakpoints for all five pages. | High |
